# Trees

## Binary Tree Applications

- Binary trees as expression trees are used in compilers
- It is also used in Huffman coding in data compression
- Binary search trees are used for efficient searching, insertion, and deletion of a list of items
- Priority Queue (PQ), which is used for finding and deleting minimum or maximum items in a collection of elements in
  logarithm time in the worst case
